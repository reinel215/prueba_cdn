{
  "name": "generar-api_key",
  "version": "1.0.0",
  "description": "CDN for consumers buissness",
  "main": "index.js",
  "scripts": {
    "build": "babel ./index.js --out-file ./out/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "reinel arteaga",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11"
  },
  "dependencies": {
    "@babel/polyfill": "^7.12.1"
  }
}
